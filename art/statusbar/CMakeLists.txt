set(DATAFILES
        network-error.png
        network-idle.png 
        network-offline.png
        network-receive.png 
        network-transmit.png
        network-transmit-receive.png)

install(FILES ${DATAFILES}         DESTINATION share/wxMaxima)
